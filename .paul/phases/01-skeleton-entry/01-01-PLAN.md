---
phase: 01-skeleton-entry
plan: 01
plan_type: execute
wave: 1
depends_on: []
files_modified: ["src/midas.md", "~/.claude/commands/midas.md", "~/.base-frameworks/midas/", "README.md"]
autonomous: true
description: "Source tree + the single thin router command + installed surfaces, per skillsmith canonical installed shape"
type: Plan
about: "12-ops-midas"
---

<objective>
## Goal
The MIDAS source repo has its full folder tree and a spec-conformant thin router
entry point; `/midas` exists as exactly ONE flat menu entry routing by absolute
path into `~/.base-frameworks/midas/`.

## Purpose
Everything later phases author must have a home (tree) and a way to be reached
(router). The router IS the product's front door — menu hygiene is a Definition
of Done item.

## Output
- `src/midas.md` — the router (source of truth)
- `src/README.md` — repo orientation
- Installed: `~/.claude/commands/midas.md` (flat file) + `~/.base-frameworks/midas/` skeleton
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source Files
@~/chris-ai-systems/planning/devops-security-framework/01-ARCHITECTURE.md (§5 shape, §4 stances)
@~/ops-sys/toolbox/frameworks/02-kit-skillsmith/specs/entry-point.md (incl. new Installed Shape section)
@~/.claude/commands/high-signal-extraction.md (reference implementation of the pattern)
</context>

<acceptance_criteria>

## AC-1: Router is spec-conformant
```gherkin
Given the patched skillsmith entry-point spec and entry-point-rules
When src/midas.md is validated against them
Then it has full frontmatter (name, type: suite, version 0.1.0, category: operations, description, allowed-tools incl. Bash)
And all 5 XML sections in order (activation, persona, commands, routing, greeting)
And every auxiliary reference uses absolute ~/.base-frameworks/midas/… paths
And it contains detection-first routing logic with no process logic
```

## AC-2: Menu hygiene holds
```gherkin
Given the installed surfaces
When ~/.claude/commands/ is listed
Then midas.md exists as a single flat FILE (not a directory)
And no midas/ subdirectory exists anywhere under ~/.claude/commands/
```

## AC-3: Routing targets resolve
```gherkin
Given the router's command table and routing section
When each referenced path under ~/.base-frameworks/midas/ is checked
Then the directory skeleton for every referenced folder exists
And the per-app context file location (context/) exists for detection to read
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author the MIDAS router (src/midas.md) + README</name>
  <files>~/ops-sys/toolbox/frameworks/12-ops-midas/src/midas.md, ~/ops-sys/toolbox/frameworks/12-ops-midas/README.md</files>
  <action>
    Write the router per the skillsmith entry-point spec + Installed Shape section:
    - Frontmatter: name: midas, type: suite, version: 0.1.0, category: operations,
      description (one-line: opinionated DevOps + security/compliance delivery framework),
      allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, AskUserQuestion]
    - <activation>: What (delivery+hardening pipeline, aegis inside), When to Use
      (new app to production, posture audit, pipeline/secrets/deploy/harden/compliance work),
      Not For (feature building → PAUL; deep code audit alone → standalone /aegis; formal certification)
    - <persona>: opinionated senior SRE + compliance-minded principal engineer who
      TEACHES while executing — every action explains the why + the control it satisfies;
      deny-by-default instincts; anti-hope ("machine-verified truth" language)
    - <commands>: table of 9 sub-commands (/midas assess|provision|pipeline|secrets|
      deploy|smoke|harden|compliance|incident) → ~/.base-frameworks/midas/tasks/{name}.md,
      plus /midas (bare) → detection
    - Detection rules: read ~/.base-frameworks/midas/context/app-registry.md;
      no entry for cwd app → assess; registered but no pipeline → pipeline; explicit
      subcommand honored directly
    - <routing>: Always-on = context/app-registry.md; Load on Command = the 9 task
      files (absolute paths); Load on Demand = frameworks/*.md, aegis/ (named
      individually); note that each task declares its own framework/checklist needs
    - <greeting>: MIDAS loaded + the golden-touch one-liner + the 9 actions + prompt
    Keep it thin — identity + routes + detection only; no workflow steps.
    <!-- audit-added --> Missing-route rule in <routing>: if a routed file does not
    exist at ~/.base-frameworks/midas/…, SAY SO and stop — never improvise the
    workflow from memory (silent-failure prevention, mirrors §A protocol discipline).
    README.md: what this repo is, source-of-truth note, installed surfaces, install
    procedure (cp commands), <!-- audit-added --> uninstall procedure (rm the command
    file + rm -rf ~/.base-frameworks/midas), sibling/toolbox convention, canonical
    plan pointer.
    Avoid: relative @-refs in the router (it installs alone into commands/); process
    logic (belongs in tasks/); any commands subdirectory.
  </action>
  <verify>Checklist against specs/entry-point.md: frontmatter fields present; 5 sections in order; grep confirms zero relative @-references to tasks/frameworks; file ≲120 lines</verify>
  <done>AC-1 satisfied: router validates against patched spec + rules</done>
</task>

<task type="auto">
  <name>Task 2: Install surfaces + verify menu hygiene</name>
  <files>~/.claude/commands/midas.md, ~/.base-frameworks/midas/</files>
  <action>
    <!-- audit-added --> PRE-FLIGHT (must run first): if ~/.claude/commands/midas.md,
    ~/.claude/commands/midas/ (dir), or a non-empty ~/.base-frameworks/midas/ already
    exists, STOP and surface what's there — never silently clobber shared surfaces.
    - mkdir -p ~/.base-frameworks/midas/{tasks,frameworks,agents,checklists,templates,context,aegis}
    - Seed ~/.base-frameworks/midas/context/app-registry.md — MUST declare the
      registry entry schema as its format contract: <!-- audit-added -->
      app (kebab name) · path (absolute) · stage (assessed|provisioned|piped|hardened|compliant) ·
      last_assessed (date) · posture_score (0-100 or "-") — one table row per app.
      All nine tasks read/write THIS format; no task invents its own.
    - Mirror the seed into src/context/ so the repo stays source of truth
    - cp src/midas.md ~/.claude/commands/midas.md
    - Verify: test -f ~/.claude/commands/midas.md && test ! -d ~/.claude/commands/midas
  </action>
  <verify>ls -la ~/.claude/commands/midas.md shows a flat file; find ~/.claude/commands -name "midas" -type d returns nothing; all 7 skeleton dirs exist under ~/.base-frameworks/midas/</verify>
  <done>AC-2 and AC-3 satisfied: one flat menu entry, all routing targets exist</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- ~/.claude/commands/aegis/ (standalone aegis untouched — hard constraint)
- ~/ops-sys/toolbox/frameworks/04-cai-aegis/ (upstream aegis is read-only this phase)
- Any other ~/.claude/commands/ entry

## SCOPE LIMITS
- No task/framework/checklist/template content this phase (Phases 2–4)
- No aegis vendoring (Phase 5)
- Router references files that don't exist yet — skeleton dirs are enough for AC-3

</boundaries>

<verification>
Before declaring plan complete:
- [ ] <!-- audit-added --> Pre-flight collision check ran before any install write
- [ ] <!-- audit-added --> app-registry.md declares the entry schema (format contract)
- [ ] src/midas.md passes entry-point-rules checklist (incl. new Installed Shape rules)
- [ ] ~/.claude/commands/midas.md is a flat file, byte-identical to src/midas.md
- [ ] No midas directory under ~/.claude/commands/
- [ ] ~/.base-frameworks/midas/ has all 7 skeleton dirs + context/app-registry.md
- [ ] git commit in 12-ops-midas
</verification>

<success_criteria>
- All tasks completed, all ACs verified
- Menu gains exactly one entry
- Standalone aegis untouched
</success_criteria>

<output>
After completion, create `.paul/phases/01-skeleton-entry/01-01-SUMMARY.md`
</output>
