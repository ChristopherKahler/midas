---
phase: 03-core-tasks
plan: 01
plan_type: execute
wave: 1
depends_on: ["02-01"]
files_modified: ["src/tasks/assess.md", "src/tasks/pipeline.md", "src/tasks/smoke.md", "src/tasks/secrets.md", "src/tasks/harden.md", "~/.base-frameworks/midas/tasks/"]
autonomous: true
description: "The critical five tasks — assess, pipeline, smoke, secrets, harden — per skillsmith tasks spec"
type: Plan
about: "12-ops-midas"
---

<objective>
## Goal
The five tasks encoding the hardest-won lessons exist as executable, teaching
workflows: assess (the entry point), pipeline + smoke (delivery gates),
secrets + harden (security spine).

## Purpose
These five ARE the session's proven value. Every one maps to bugs actually hit
or controls actually shipped in graph-portal.

## Output
src/tasks/{assess,pipeline,smoke,secrets,harden}.md + installed copies.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/phases/02-reference-frameworks/02-01-SUMMARY.md (framework files these load)

## Source Files
@~/chris-ai-systems/planning/devops-security-framework/02-PLAYBOOKS-AND-CONTROLS.md (§B playbook definitions)
@~/ops-sys/toolbox/frameworks/02-kit-skillsmith/specs/tasks.md (format contract)
</context>

<acceptance_criteria>

## AC-1: Spec conformance
```gherkin
Given the skillsmith tasks spec
When each of the five files is validated
Then it has no frontmatter and all required XML sections in order (purpose, user-story, when-to-use, steps, output, acceptance-criteria)
And steps are snake_case named with wait points where user input is needed
And frameworks are lazy-loaded via <references> with conditional notes, never preloaded
```

## AC-2: The MIDAS task contract
```gherkin
Given any of the five tasks
When its steps are read
Then it teaches (explains why + the control satisfied at each substantive action)
And it ends with its checklist gate phrased fail-closed
And it updates the app-registry row per the schema contract
And it records learnings/decisions to BASE where they emerge
```

## AC-3: Installed and loadable
```gherkin
Given the installed surface
When ~/.base-frameworks/midas/tasks/ is listed
Then all five exist byte-identical to src/ and match the router's command table paths
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author assess.md — the posture audit entry point</name>
  <files>src/tasks/assess.md</files>
  <action>
    Doc 2 §B1: inventory (hosting, envs, pipeline, secrets handling, migrations,
    audit logging, auth model, data classification, subprocessors) → score
    against security-controls.md domains + compliance readiness → output posture
    report + prioritized gap list to ~/.base-frameworks/midas/context/{app}/posture.md.
    Creates the app's registry row (stage: assessed, posture_score, last_assessed).
    Teaching notes per domain scored. This is where every app starts.
    <!-- audit-added --> Rubric (stated in the report): posture_score = % of
    APPLICABLE catalog controls passing, rounded; N/A controls excluded and listed
    with reasons. Re-run semantics: assess is idempotent — re-scores and updates
    the row; it is the ONLY task that may move posture_score down.
  </action>
  <verify>Spec-section grep passes; report format specified in <output>; registry write step present</verify>
  <done>AC-1, AC-2 for assess</done>
</task>

<task type="auto">
  <name>Task 2: Author pipeline.md + smoke.md — the delivery gates</name>
  <files>src/tasks/pipeline.md, src/tasks/smoke.md</files>
  <action>
    pipeline.md (§B3): branch↔env mapping, test job on prod-parity DB, browser
    smoke gate, Wait-for-CI, post-deploy live smoke, native GitHub integration +
    env-scoped token fallback. Gate: no deploy without green tests + green smoke.
    smoke.md (§B6): author the app's Playwright smoke spec — routes, auth, the
    four failure detectors, serial execution, state restoration, app-origin
    scoping; wire pre-deploy + post-deploy. Gate: smoke green locally against a
    served prod build. Both lazy-load testing-gates.md + railway.md.
  </action>
  <verify>Spec-section grep passes; four detectors named in smoke.md; fail-closed gate language in both</verify>
  <done>AC-1, AC-2 for pipeline + smoke</done>
</task>

<task type="auto">
  <name>Task 3: Author secrets.md + harden.md — the security spine; install all five</name>
  <files>src/tasks/secrets.md, src/tasks/harden.md, ~/.base-frameworks/midas/tasks/</files>
  <action>
    secrets.md (§B4): interactive provisioning (never echo/paste in session),
    env-scoped deploy tokens, rotation cadence, spawn-env safety, secret-in-code/
    log scan. Gate: scan clean + rotation documented.
    <!-- audit-added --> INVARIANT stated at every step touching a value: secret
    values are entered interactively by the OPERATOR (hidden prompts, dashboard),
    never echoed, never asked for in session, never written to any file or output.
    <!-- audit-added --> Re-run semantics for both: harden re-walk skips controls
    already satisfied after re-checking their evidence; stage never regresses
    except via assess re-score.
    harden.md (§B7): walk security-controls.md domain by domain, apply what's
    missing, each control explained (purpose + compliance criterion). Gate:
    security checklist green with evidence. Lazy-loads security-controls.md +
    protocols.md.
    Install: cp all five to ~/.base-frameworks/midas/tasks/, diff clean.
  </action>
  <verify>diff -r src/tasks ~/.base-frameworks/midas/tasks clean; router command-table paths all resolve to real files for these five</verify>
  <done>AC-1, AC-2, AC-3</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/midas.md, src/frameworks/* (shipped)
- ~/.claude/commands/ (no menu changes)

## SCOPE LIMITS
- provision/deploy/compliance/incident land in 03-02
- Checklist FILES land in Phase 4 — tasks reference them by installed path and treat a missing checklist as a FAILED gate (fail-closed), never skippable

</boundaries>

<verification>
Before declaring plan complete:
- [ ] 5/5 files pass tasks-spec section check
- [ ] Fail-closed gate language present in all five
- [ ] Registry update step present in all five
- [ ] Installed copies byte-identical; router paths resolve
- [ ] git commit
</verification>

<success_criteria>
- All ACs pass; the five tasks are executable teaching workflows
</success_criteria>

<output>
After completion, create `.paul/phases/03-core-tasks/03-01-SUMMARY.md`
</output>
