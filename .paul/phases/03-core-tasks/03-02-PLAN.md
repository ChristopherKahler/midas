---
phase: 03-core-tasks
plan: 02
plan_type: execute
wave: 2
depends_on: ["03-01"]
files_modified: ["src/tasks/provision.md", "src/tasks/deploy.md", "src/tasks/compliance.md", "src/tasks/incident.md", "~/.base-frameworks/midas/tasks/"]
autonomous: true
description: "Remaining four tasks (provision, deploy, compliance, incident) + graph-portal dry-run validation"
type: Plan
about: "12-ops-midas"
---

<objective>
## Goal
All nine router commands resolve to real, spec-conformant task files, and the
assess + pipeline guidance is validated against graph-portal — the app whose
every ops decision we lived.

## Purpose
Completes the executable layer and proves it against ground truth before the
enforcement artifacts (Phase 4) are built on top.

## Output
src/tasks/{provision,deploy,compliance,incident}.md + installed; dry-run findings note.
</objective>

<context>
## Project Context
@.paul/phases/03-core-tasks/03-01-SUMMARY.md (contract patterns: fail-closed, registry, invariant)

## Source Files
@~/chris-ai-systems/planning/devops-security-framework/02-PLAYBOOKS-AND-CONTROLS.md (§B2, B5, B8, B9)
@~/chris-ai-systems/apps/graph-portal (dry-run reference app)
</context>

<acceptance_criteria>

## AC-1: Spec + contract conformance
```gherkin
Given the four new task files
When validated like 03-01's five
Then all required XML sections present, fail-closed gates, registry updates, teaching notes
And the secrets non-echo invariant is restated wherever a task touches credentials
```

## AC-2: Router completeness
```gherkin
Given the installed tasks/
When the router's nine command paths are checked
Then all nine resolve to real files
```

## AC-3: Dry-run fidelity
```gherkin
Given graph-portal's actual CI, deploy setup, and known history
When assess.md's inventory+scoring steps and pipeline.md's wiring steps are walked against it (read-only)
Then the guidance matches what was actually done (or the mismatch is a real finding recorded for Phase 6)
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Author provision.md + deploy.md</name>
  <files>src/tasks/provision.md, src/tasks/deploy.md</files>
  <action>
    provision.md (§B2): project + Postgres + volumes, env ladder with ISOLATED
    databases, per-env domains, deny-by-default posture. Gate: no shared data
    stores across tiers.
    deploy.md (§B5): ladder discipline (dev free, stage deliberate, main
    ceremonial), migrations downstream-first (A10), rollback (redeploy prior
    known-good + DB caveats), hotfix path (branch off prod, merge DOWN to
    stage+dev to prevent drift). Gate: rollback tested, hotfix path documented.
    <!-- audit-added --> "Rollback tested" DEFINED: deploy known-good N, deploy
    N+1, roll back to N, live smoke green on N — with the migration-asymmetry
    caveat (schema rollbacks are not symmetrical; roll forward when in doubt).
    Both lazy-load railway.md; deploy also protocols.md (A10).
  </action>
  <verify>Section grep + fail-closed + registry steps present</verify>
  <done>AC-1 for these two</done>
</task>

<task type="auto">
  <name>Task 2: Author compliance.md + incident.md; install all four</name>
  <files>src/tasks/compliance.md, src/tasks/incident.md, ~/.base-frameworks/midas/tasks/</files>
  <action>
    compliance.md (§B8): crosswalk implemented controls → target regime via
    compliance-maps.md; generate the evidence index (control → satisfied-by →
    evidence location) to context/{app}/evidence.md; readiness report an exec's
    security team can read; honest ready-not-certified language; BAA hard gate
    surfaced when PHI flagged. Gate: no unmapped high-risk control.
    incident.md (§B9): detection (audit logs + smoke/health), containment,
    user-communication path, post-incident reconstruction from the immutable
    audit trail, post-incident protocol capture (new protocol → protocols.md).
    Gate: audit trail proven sufficient to reconstruct who-did-what-when.
    <!-- audit-added --> SEVERITY FIRST: classify before containment —
    data-exposure incidents make user comms + the notification clock (GDPR 72h)
    MANDATORY; availability-only incidents leave comms to judgment.
    Install all four; diff clean; nine router paths resolve.
  </action>
  <verify>diff -r clean; nine/nine router paths resolve</verify>
  <done>AC-1, AC-2</done>
</task>

<task type="auto">
  <name>Task 3: Dry-run assess + pipeline guidance against graph-portal (read-only)</name>
  <files>.paul/phases/03-core-tasks/03-02-DRYRUN.md</files>
  <action>
    Read-only walk: run assess.md's inventory steps against
    ~/chris-ai-systems/apps/graph-portal (CI workflows, Dockerfile,
    railway.json, playwright config, migrations) and check pipeline.md's
    wiring guidance against what graph-portal actually has. Record: guidance
    matches reality? Gaps in the TASK (fix now) vs gaps in the APP (real
    findings — park for Phase 6 dogfood). Do NOT modify graph-portal.
  </action>
  <verify>DRYRUN.md records per-step match/mismatch with file evidence</verify>
  <done>AC-3</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- graph-portal (READ-ONLY this plan — findings park for Phase 6)
- Shipped MIDAS files from prior plans (except bugs the dry-run exposes in 03-01 tasks)

## SCOPE LIMITS
- Checklist/template FILES are Phase 4
- No aegis wiring yet (Phase 5) — compliance.md references the vendored aegis path with the missing-route rule in force

</boundaries>

<verification>
Before declaring plan complete:
- [ ] 4/4 new files pass the spec + contract checks
- [ ] 9/9 router paths resolve
- [ ] DRYRUN.md written with evidence
- [ ] git commit
</verification>

<success_criteria>
- Executable layer complete and reality-checked
</success_criteria>

<output>
After completion, create `.paul/phases/03-core-tasks/03-02-SUMMARY.md`
</output>
